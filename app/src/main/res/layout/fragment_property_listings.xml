<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/fragmentListings"
    android:background="@color/cardview_dark_background">


    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_property_listings"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical"
        android:paddingTop="@dimen/rvPaddingTop"
        android:paddingLeft="@dimen/rvPaddingLeft"
        android:paddingRight="@dimen/rvPaddingRight"
        />

        <!-- TODO have a look at this -->
    <include layout="@layout/view_progress" />

    <include layout="@layout/view_retry" />

</android.support.v4.widget.SwipeRefreshLayout>


    </RelativeLayout>